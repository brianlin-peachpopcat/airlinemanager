// Obsolete one-off rename helper. Live saves use the encoded legacy remap
// in js/game.js (RENAMED_CARRIERS). Do not reintroduce real-world carrier
// or manufacturer trademarks as plaintext here.
console.log("rename_brands.js is retired — see RENAMED_CARRIERS in js/game.js.");
process.exit(0);
